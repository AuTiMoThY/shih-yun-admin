export default defineAppConfig({
    ui: {
        colors: {
            primary: "blue",
            neutral: "slate"
        }
    },
    colorMode: {
        preference: "system" // 可選：'system' | 'light' | 'dark'
    }
});
